# minforest
## What is it?
- A library implementing Decision Trees and Random Forest
- Optimised for readability and understanding
- More for education rather than real-world use

Example:
```py
tree = mf.DecisionTree(x, y)
preds = tree.predict(x_test)
```